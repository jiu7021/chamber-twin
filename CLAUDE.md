# etch-chamber-vacuum-twin

반도체 식각 챔버 진공 배기 상태를 압력·유량 데이터로 진단하는 시스템.
공개 실측 데이터(Zenodo DOI 10.5281/zenodo.17122442)로 유효 배기속도를 역산하고,
검증된 물리모델로 고장 시나리오를 시뮬레이션하는 인터랙티브 웹 시뮬레이터.

## 단위 규약 (절대 준수)

내부 계산은 SI 단일계로만 수행한다.

| 물리량 | 단위 |
|---|---|
| 압력 | Pa |
| 부피 | m³ |
| 배기속도 | m³/s |
| 스루풋(가스 유량) | Pa·m³/s |
| 시간 | s |
| 온도 | K |

- 환산(mTorr, sccm, L/s 등)은 입출력 경계(입력 파싱, 출력 표시)에서만 수행한다. 중간 계산에 섞지 않는다.
- 환산 상수는 `physics/units.py` 한 곳에 모으고, 각 상수에 유도 과정을 주석으로 단다.
  - 1 Torr = 133.322 Pa
  - 1 mbar = 100 Pa
  - 1 sccm = 1.69e-3 Pa·m³/s (1 cm³/min @ 101325 Pa = 1.667e-8 m³/s × 101325 Pa)
  - 1 L/s = 1e-3 m³/s
- 모든 함수 docstring에 단위를 명시한다.
- 단위 환산 왕복 테스트(A → 변환 → 역변환 → A)를 pytest에 포함한다.

## 물리 파라미터 규칙

출처 없는 상수를 채워 넣지 않는다. 모든 상수는 다음 중 하나여야 하며 코드에 출처 주석을 단다.
- (물성치) 데이터에서 직접 측정/역산한 값
- (통상범위) 논문·스펙시트에서 인용한 값 — 출처 명시
- (표준·규정) 규격에 명시된 값
- (가정치) 근거를 못 찾아 사용자가 지정한 값 — 반드시 "가정치"로 표기하고 민감도 분석 대상에 포함

근거를 찾지 못하면 지어내지 말고 멈추고 사용자에게 확인한다.

## 검증 게이트

각 Phase는 정해진 검증 기준을 통과해야 다음 Phase로 진행한다. 실패 시 결과를 그대로 보고하고 멈춘다.
기준 완화, 데이터 선별, "대체로 잘 맞는다"로 넘어가는 것은 금지한다.

## 데이터

- `data/`는 `.gitignore` 대상이다. 저장소에는 `scripts/download.sh`만 커밋한다.
- OES 스펙트럼(3648채널)은 이 프로젝트에서 사용하지 않는다.
